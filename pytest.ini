[pytest]
# repo root on sys.path so `apps.` / `src.` import without an install
pythonpath = .
testpaths = tests
addopts = -ra -q
markers =
    llm: test calls the live shared vLLM — slow/non-deterministic, opt-in via --run-llm
